name: Deployment

on:
  push:
    branches:
      - flostream-v2

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: SSH Deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PASSWORD }}
          script: |
            export NVM_DIR=~/.nvm
            source ~/.nvm/nvm.sh
            nvm use 20
            cd flostream-storage
            git pull origin flostream-v2
            cp .env.example .env
            yarn
            yarn build
            pm2 restart flostream-storage
